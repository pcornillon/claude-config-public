<!-- CONTRACT: living snapshot of where things stand RIGHT NOW. Rewritten, never
     appended — it is not a history. It must always end with "## Active thread —
     resume here", even when nothing is in flight (then it says so). A per-item
     deliverables tracker is NOT this file; give that its own name. -->

# STATUS.md — `{{PROJECT}}`

Living snapshot. Update when the state changes. Last updated: **{{DATE}}**.

---

## State

Nothing built yet — project scaffolded {{DATE}}.

## Active thread — resume here

Nothing in flight. Next: fill in `CLAUDE.md`'s "what this project is" and add the
first tasks.
