<!-- CONTRACT: numbered work list. Tasks are appended, never renumbered and never
     reopened — a change requested after a task is done is a NEW task referencing it.
     Each carries a Status: line. Claude maintains the Status lines; {{OWNER}} never has
     to open this file to add work, though he may. -->

# TASKS.md — `{{PROJECT}}`

**Prerequisite:** read `CLAUDE.md`, then `STATUS.md`.

**Governing principle:** propose structural changes for approval — never apply them
unasked.

---

# Guardrails

{{GUARDRAILS}}

# Conventions

- `Status:` line per task — `todo`, `doing`, `blocked (on what)`, `done (YYYY-MM-DD)`.
- Tasks are never reopened; a follow-on change is a new task referencing the old one.

---

## Task #1 — {{FIRST_TASK}}

**Status:** todo
