<!-- CONTRACT: living snapshot of where things stand RIGHT NOW. Rewritten, never
     appended — it is not a history. It must always end with "## Active thread —
     resume here", even when nothing is in flight (then it says so). -->

# STATUS.md — `claude-config-public`

Living snapshot. Update when the state changes.

---

## Active thread — resume here

Nothing in flight yet. First job: run `./install.sh`, then read
`global/CLAUDE.md` and make it yours.
